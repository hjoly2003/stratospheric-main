spring:
  application:
    name: demo-application
  config:
    import: 'aws-parameterstore:/config/app'
  cloud:
    aws:
      credentials:
        profile:
          name: stratospheric
      region:
        static: us-east-1

custom:
  bucket-name: stratospheric-demo-application

